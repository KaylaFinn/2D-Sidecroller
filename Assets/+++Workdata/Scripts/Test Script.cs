using UnityEngine;

public class TestScript : MonoBehaviour
{
    //(optional: Zugriffswert) Variablentypen      Benennung      (optional: Wertzuweisung)
    public                     float               walkingSpeed    = 5f;
    
    /*
     * inf = ganze Zahlen
     * float = #Decimalzahlen
     * string = Zeichenketten -> ABCdef!?10
     * bool = wahr oder falsch
     */

    //(optional: Zugriffswert) Variablentypen     Bennenung      (optional : Wertzuweisung)
    public                     float              walkingSpeed    = 5f;

    public int playerLevel = 5;
    
    //booloan
    public bool isGrounded; // true // false

    public string playerName = "5";
    
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
